import 'package:flutter/material.dart';

void main() {
  runApp(UdemyStarter());
}

class UdemyStarter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Text("hello"),
    );
  }
}
